syntax = "proto3";

option csharp_namespace = "GrpcService";

package greet;

service ChatRoom {
	rpc join(stream Message) returns (stream Message) {}
}

message Message {
	string user = 1;
	string text = 2;
}